var attr = DS.attr;
App.UserItemList = DS.Model.extend({
    payload: DS.hasMany('userOwnership'),
    success: attr('string'),

    hasError: function () {
        var currentError = this.get("error");
        return !(currentError === '' || currentError == null);
    }.property('error'),

});

